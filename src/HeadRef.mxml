<?xml version="1.0" encoding="utf-8"?>
<s:ViewNavigatorApplication xmlns:fx="http://ns.adobe.com/mxml/2009" 
							xmlns:s="library://ns.adobe.com/flex/spark" 
							firstView="views.HeadRefHomeView" 
							applicationDPI="160" 
							navigatorStateSaving="onStateSave(event)"
							navigatorStateLoading="onStateLoad(event)" 
							preinitialize="preinitializeHandler(event)" >
	
	<fx:Style source="style/headRef.css" />
	
	<fx:Script>
		<![CDATA[
			import model.GameModel;
			import model.HeadRefModel;
			import model.LeagueRules;
			import model.TeamModel;
			
			import mx.events.FlexEvent;
			
			import spark.managers.PersistenceManager;
						
			protected function onStateSave(event:FlexEvent):void{
				// TODO Auto-generated method stub
				
			}
			
			protected function onStateLoad(event:FlexEvent):void{
				// TODO Auto-generated method stub
				
			}
			
			protected function preinitializeHandler(event:FlexEvent):void{
				flash.net.registerClassAlias("headRefModel", HeadRefModel);
				flash.net.registerClassAlias("teamModel", TeamModel);
				flash.net.registerClassAlias("leagueRules", LeagueRules);
				flash.net.registerClassAlias("gameModel", GameModel);
				persistenceManager.load();
			}
			
		]]>
	</fx:Script>
	
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
</s:ViewNavigatorApplication>
